rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Разрешить чтение и запись только залогиненным пользователям
      allow read, write: if request.auth != null;
    }
  }
}
